import { RulerDirective } from './ruler.directive';

describe('RulerDirective', () => {
  it('should create an instance', () => {
    const directive = new RulerDirective();
    expect(directive).toBeTruthy();
  });
});

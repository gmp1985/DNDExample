import { ImagedragdropDirective } from './imagedragdrop.directive';

describe('ImagedragdropDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagedragdropDirective();
    expect(directive).toBeTruthy();
  });
});
